# Python artifacts
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# Virtual environment — never copy into the image
.venv/
venv/

# Build / dist (frontend is built inside Docker; don't ship local artifacts)
build/
dist/

# Node / frontend dependencies (installed inside Docker)
node_modules/

# Git
.git/
.gitignore

# IDE / OS
.DS_Store
*.swp
.idea/
.vscode/

# CI / local cache
.cache/
